#include <iostream>
using namespace std;

int main() {
    int x = 1;
    int y = 2;
    cout<<x+y<<endl;

    return 0;
}

/* Datatypes: 
int, short, long: Stores integers, but with different ceilings and floors. (bytes) 
unsigned int: Integers without negatives, which allows the ceiling to be twice as large as int's ceiling
float, double: Stores floating point numbers, but with different ceilings and floors. (bytes)
char: Stores a character
boolean: Stores a True or a False

const: This variable cannot be changed
void: 無值
*/